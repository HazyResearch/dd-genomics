SELECT 
  a.gene_entity, pheno_entity, count(*) 
FROM 
(SELECT gene_entity, pheno_entity FROM genepheno_association
WHERE 
  is_correct != 'f' OR is_correct IS NULL 
) a
GROUP BY gene_entity, pheno_entity
ORDER BY count DESC
LIMIT 1000
