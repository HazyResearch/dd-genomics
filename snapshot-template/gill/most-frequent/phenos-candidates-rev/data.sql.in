SELECT 
  entity, hpo.pheno_name, count(entity)
FROM pheno_mentions
  JOIN
  hpo_abnormalities hpo
    ON (hpo.hpo_id = pheno_mentions.entity)
WHERE
  is_correct != 'f' OR is_correct IS NULL
GROUP BY entity, hpo.pheno_name
ORDER BY count ASC
LIMIT 1000
