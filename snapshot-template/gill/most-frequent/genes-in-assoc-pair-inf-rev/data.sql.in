SELECT 
  g.canonical_name, count(g.canonical_name)
FROM genepheno_association_is_correct_inference ga join genes g on (ga.gene_name = g.gene_name)
WHERE 
  ga.expectation > 0.9
GROUP BY g.canonical_name
ORDER BY count ASC
LIMIT 1000
