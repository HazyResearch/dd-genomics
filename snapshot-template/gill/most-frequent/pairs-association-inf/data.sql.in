SELECT 
  canonical_name, pheno_entity, count(*) 
FROM 
(SELECT g.canonical_name, ga.pheno_entity FROM genepheno_association_is_correct_inference ga join genes g on (ga.gene_name = g.gene_name)
WHERE 
  ga.expectation > 0.9
) a
GROUP BY canonical_name, pheno_entity
ORDER BY count DESC
