SELECT 
  g.canonical_name, count(g.canonical_name)
FROM genepheno_causation_is_correct_inference gc join genes g on (gc.gene_id = g.gene_id)
WHERE 
  gc.expectation > 0.9
GROUP BY g.canonical_name
ORDER BY count DESC
LIMIT 1000
