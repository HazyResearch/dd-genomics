SELECT 
  g.canonical_name, count(g.canonical_name)
FROM genepheno_association_is_correct_inference ga join genes g on (ga.gene_id = g.gene_id)
WHERE 
  ga.expectation > 0.9
GROUP BY g.gene_id
ORDER BY count DESC
LIMIT 1000
