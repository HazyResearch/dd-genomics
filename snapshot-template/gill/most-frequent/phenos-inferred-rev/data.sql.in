SELECT 
  entity, hpo.pheno_name, count(entity)
FROM pheno_mentions_filtered_is_correct_inference
  JOIN
  hpo_abnormalities hpo ON (hpo.hpo_id = pheno_mentions_filtered_is_correct_inference.entity)
WHERE
  expectation > 0.9
GROUP BY entity, hpo.pheno_name
ORDER BY count ASC
LIMIT 1000
