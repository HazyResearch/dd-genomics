SELECT 
  g.canonical_name, count(g.canonical_name)
FROM gene_mentions_filtered_is_correct_inference gm join genes g on (gm.gene_id = g.gene_id)
WHERE 
  gm.expectation > 0.9
GROUP BY g.canonical_name
ORDER BY count DESC
LIMIT 1000
