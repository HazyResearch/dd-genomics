SELECT 
  g.canonical_name, count(g.canonical_name) 
FROM genepheno_association ga join genes g on (ga.gene_name = g.gene_name)
WHERE 
  ga.is_correct != 'f' OR ga.is_correct IS NULL 
GROUP BY g.canonical_name
ORDER BY count ASC
LIMIT 1000
