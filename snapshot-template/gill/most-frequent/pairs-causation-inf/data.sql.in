SELECT 
  a.gene_entity, pheno_entity, count(*) 
FROM 
(SELECT gene_entity, pheno_entity FROM genepheno_causation_is_correct_inference
WHERE 
  expectation > 0.9
) a
GROUP BY gene_entity, pheno_entity
ORDER BY count DESC
LIMIT 1000
